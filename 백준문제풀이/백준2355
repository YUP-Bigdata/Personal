a, b = map(int, input().split())
n_max = max(a, b)
n_min = min(a, b)
n = n_max - n_min
s = (n * (n + 1)) // 2
print(s + (n_min * (n + 1)))

#a가 큰지 b가 큰지 모르기 때문에 큰 수 작은 수 나눠서 넣는다. 
#5와 10을 입력받았다고 했을때, 5 + 6 + 7 + 8 + 9 + 10이 된다.
#이는 5 + (5 + 1) + (5 + 2) + (5 + 3) + (5 + 4) + (5 + 5)가 될 수 있다.
#5만 따로 빼줘서 5 * ((10 - 5) + 1)을 해주고, 1부터 5까지 더한 값을 더해주면 된다.